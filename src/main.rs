use yarl_2::{run_game, Config}; //, Window

fn main() {
    println!("Hello, world!"); //game//config
    run_game((), Config::default()) //Window::new_run((),Config::default());
                                    /* loop{

                                    } */
}
